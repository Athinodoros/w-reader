import ReadingContext, { ReadingStatus } from "./utils/ReadingContext";

let context = new ReadingContext(5,5,ReadingStatus.READING,
    "this (parenthesis) is a test [(parenthesis in suqares)] {}.with 2 <diamond> sentences");


